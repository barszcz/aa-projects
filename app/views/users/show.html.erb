<%= @user.username %>'s goals

<% @user.goals.each do |goal| %>

  <% if goal.public? || goal.user_id == current_user.id %>
    <a href="<%= goal_url(goal) %>"><%= goal.aim %></a>
    <br>
    <%= "<strong>Complete!</strong>".html_safe if goal.completed? %>
  <% end %>
<% end %>
